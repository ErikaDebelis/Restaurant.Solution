@{
  Layout = "_Layout";
}

@model BestRestaurants.Models.Restaurant

<h4>Add a new Restaurant to the List!</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.RestaurantName)
  @Html.TextBoxFor(model => model.RestaurantName)

  <label for="cuisine">Enter Cuisine</label>
  <input id="cuisine" name="cuisineName" required>
  
<!-- 
  @Html.LabelFor(model => model.Cuisine)
  @Html.DropDownList("CuisineId") -->
  <input type="submit" value="Add new restaurant" />
}
@* <h4>@Html.ActionLink("Add new cuisine first", "Create", "Cuisines")</h4> *@
<h4>@Html.ActionLink("Show all restaurants", "Index")</h4>